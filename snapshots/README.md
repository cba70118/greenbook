# Weekly Snapshots

This folder stores weekly DG form data snapshots.

- `history.json` — rolling master file with all snapshots
- `sparklines.json` — processed sparkline data for the dashboard
- `YYYY-MM-DD_tournament.json` — individual weekly snapshots

Run `py -3 scripts/weekly_snapshot.py "Tournament Name"` each week to capture.
